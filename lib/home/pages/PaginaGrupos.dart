import 'package:flutter/material.dart';

class PaginaGrupos extends StatelessWidget {
  const PaginaGrupos({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
          "Grupos",
          style: TextStyle(fontSize: 30),
        ));
  }
}